const fs = require('fs');

const readStream = fs.createReadStream('./nyn/example.txt', 'utf8');
const writeStream = fs.createWriteStream('./nyn/example1.txt')

readStream.on('data', (chunk)=>{
    writeStream.write(chunk);
});